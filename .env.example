# 本地开发环境变量样例（复制为 .env 使用；.env 已被 gitignore）。
# 端口（local.entry.js 默认 8787；shell 已 export 的 PORT 会覆盖本文件）
# PORT=8787

# 字段加密密钥（必填才可启用登录与敏感字段加密；本地开发可填任意长随机串）
# APP_ENCRYPTION_KEY=change-me-to-a-long-random-string

# 日志级别（local 默认 debug，其余环境默认 warn）
# LOG_LEVEL=debug