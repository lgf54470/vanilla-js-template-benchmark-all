# docs/perf/ — 性能调查与优化记录

记录具体的性能问题调查、优化前后的实测数据。对应 `ARCHITECTURE.md §12`
的缓存与查询优化策略在真实平台上的表现——尤其是 §12.1
提到的"进程内缓存在边缘运行时是尽力而为"，这里应该积累各平台的实测命中率数据，而不是停留在理论假设。文件名
`YYYY-MM-DD-<slug>.md`。

## 模板

```markdown
# YYYY-MM-DD: <一句话描述调查的性能问题>

- 平台：<cloudflare / vercel / deno / docker>
- 触发场景：<哪个接口/页面/操作>

## 优化前

<具体指标：响应时间 P50/P95、数据库查询次数、缓存命中率等，附测量方法>

## 假设与验证

<怀疑是什么原因，怎么验证的>

## 优化手段

<具体改动，链接 PR>

## 优化后

<对比数据>

## 是否需要更新 ARCHITECTURE.md / docs/Database.md 等规范

<如果这次优化改变了通用策略（如索引策略、缓存 TTL 默认值），链接对应文档更新>
```

暂无记录。
